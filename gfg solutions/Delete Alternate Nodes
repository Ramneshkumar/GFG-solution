void deleteAlt(struct Node *head)
{
    Node *temp = head->next;
    Node *pahle = head;
    while(temp != NULL)
    {
        pahle->next = temp->next;
        pahle = temp;
        temp = temp->next;  
    }   
}
